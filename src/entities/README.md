# Domain types

Shared API response types belong here. Define the weight measurement type when the weight-service API contract is agreed, before adding its requests or mock data. Keep its shape aligned with that contract.
